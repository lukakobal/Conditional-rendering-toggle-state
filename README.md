# React 53 – Conditional Rendering & Toggle 🧩

Simple React app demonstrating **conditional rendering** using a boolean state.

## 🎯 What you learn
- Toggle state (`true / false`)
- Conditional rendering with `&&`
- How React adds/removes JSX from the DOM

## 🧠 Key concept
When the condition is `false`, JSX **is not rendered at all** – it does not exist in the DOM.

## 🛠 Tech
- React
- useState
- Functional components

## ▶️ Behavior
- Click button → panel appears
- Click again → panel is removed from the DOM

 https://codesandbox.io/p/sandbox/4qr8r8?file=%2Fsrc%2Fstyles.css%3A1%2C1-28%2C1
